{
    "name": "@klapeks/database-ui",
    "description": "Web database ui",
    "version": "0.1.3",
    "main": "dist/server/index.js",
    "author": "Klapeks",
    "license": "ISC",
    "scripts": {
        "build": "rm -rf dist && mkdir dist && cd ./server && npm run build && cd ../frontend && npm run build:copy"
    },
    "dependencies": {
    },
    "devDependencies": {
        "@types/node": "^18.7.8"
    }
}
